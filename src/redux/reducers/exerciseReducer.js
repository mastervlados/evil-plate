import { 

} from "../constants"

const initialState = {
    
}

const testState = {

}

const exerciseReducer = (state = testState, action) => {

    switch (action.type) {
        
        default:
            return state
    }
}

export default exerciseReducer