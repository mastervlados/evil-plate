import { StyleSheet } from 'react-native'
import { positive } from './generalStyles/theme'

export const styles = StyleSheet.create({
    success: {
        backgroundColor: positive,
        elevation: 3,
        alignItems: 'center',
        justifyContent: 'center',
    }
})